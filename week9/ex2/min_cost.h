#ifndef MIN_COST_H
#define MIN_COST_H  // Define macro to ensure that the header file's contents
                    // are only included once, avoiding potential redefinition
                    // errors.

#include <cmath>
#include <queue>
#include <unordered_map>
#include <vector>

using namespace std;

int minCost(vector<vector<int>> &points);

#endif  // MIN_COST_H
