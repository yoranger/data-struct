#ifndef VALID_PATH_H
#define VALID_PATH_H // Define macro to ensure that the header file's contents are only included once, avoiding potential redefinition errors.

#include <vector>
#include <queue>

using namespace std;

bool validPath(int n, vector<vector<int>>& edges, int source, int destination);

#endif // VALID_PATH_H
