#ifndef CONNECTED_COMPONENT_H
#define CONNECTED_COMPONENT_H // Define macro to ensure that the header file's contents are only included once, avoiding potential redefinition errors.

#include <vector>

using namespace std;

int countComponents(int n, vector<vector<int>>& edges);

#endif // CONNECTED_COMPONENT_H
